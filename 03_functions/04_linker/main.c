#include <stdio.h>
#include "math_utils.h"

int main(void){

    int addt = addition(45,11);
    int subt = subtraction(99,10);
    printf("the addition: %d\n",addt);
    printf("the subtraction: %d\n",subt);
    return 0;

}